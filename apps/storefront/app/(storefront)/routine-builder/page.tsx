import { RoutineBuilderView } from "@/components/routine/RoutineBuilderView";

export const metadata = { title: "Routine builder" };

export default function RaysRoutineBuilderPage() {
  return <RoutineBuilderView variant="rays" />;
}

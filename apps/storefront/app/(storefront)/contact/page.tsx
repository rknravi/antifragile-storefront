import { ContactPageView } from "@/components/content/PolicyViews";

export const metadata = { title: "Contact", description: "Contact ANTIFRAGILE / RKN Overseas." };

export default function RaysContactPage() {
  return <ContactPageView variant="rays" />;
}
